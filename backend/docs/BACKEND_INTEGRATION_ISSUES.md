# 백엔드 통합 이슈 추적

> **작성일**: 2025-10-31
> **관리**: 백엔드 개발팀

---

## 📌 이슈 상태 코드

- 🔴 **Open**: 미해결 이슈
- 🟡 **In Progress**: 작업 중
- 🟢 **Resolved**: 해결 완료
- ⚪ **Closed**: 종료 (검증 완료)

---

## 🔴 현재 미해결 이슈

### 없음

현재 프론트엔드 통합과 관련하여 미해결 이슈가 없습니다.

---

## ⚪ 해결 완료 이슈

### ISSUE-001: 골프 스윙 swingType 파라미터 미구현 ⚪

**우선순위**: 🔴 높음
**발견일**: 2025-10-31
**해결일**: 2025-10-31
**담당자**: 백엔드 개발팀

**문제 설명**:
- API 명세서에 `swingType` ("full" | "half") 파라미터 명시됨
- 프론트엔드에서 스윙 타입 선택 UI 구현 완료
- 백엔드에서 해당 파라미터를 전혀 받지 않음
- 사용자가 선택한 스윙 타입이 무시되는 문제

**영향**:
- 풀스윙/하프스윙 구분 불가
- 분석 결과에 스윙 타입이 반영되지 않음
- 프레임 정보가 잘못 표시될 가능성

**해결 방법**:
1. `GolfSwingController.uploadVideo()`에 `swingType` 파라미터 추가
2. 유효성 검증 로직 구현 (값이 "full" 또는 "half"가 아니면 400 에러)
3. `UploadSwingVideoUseCase.execute()`에 `swingType` 전달
4. `SwingTypeEntity` 레코드 자동 생성 로직 추가

**수정 파일**:
- `src/presentation/controllers/golf-swing.controller.ts`
- `src/application/use-cases/golf-swing/UploadSwingVideoUseCase.ts`

**테스트 결과**:
```bash
✅ swingType 누락 시: 400 에러 반환
✅ 잘못된 값 ('middle' 등): 400 에러 반환
✅ 유효한 값 ('full', 'half'): 정상 처리
✅ 데이터베이스 저장 확인: golf_swing_types 테이블에 정상 저장
```

**참고 문서**:
- `FRONTEND_INTEGRATION_SUMMARY.md` - 이슈 #1
- `BACKEND_INTEGRATION_STATUS.md` - 완료 작업 #1

---

### ISSUE-002: CORS 설정 불일치 ⚪

**우선순위**: 🔴 높음
**발견일**: 2025-10-31
**해결일**: 2025-10-31
**담당자**: 백엔드 개발팀

**문제 설명**:
- 백엔드 CORS 설정: `http://localhost:3000` 허용
- 프론트엔드 개발 서버: `http://localhost:5173` (Vite 기본 포트)
- 포트 불일치로 인한 CORS 에러 발생 가능성

**영향**:
- 프론트엔드에서 API 호출 시 CORS 에러 발생
- 통합 테스트 불가

**해결 방법**:
1. `.env` 파일의 `FRONTEND_URL` 수정: `http://localhost:5173`
2. 서버 재시작하여 변경사항 적용

**수정 파일**:
- `.env`

**테스트 결과**:
```bash
✅ CORS 설정 확인: http://localhost:5173 허용
✅ 서버 로그 확인: CORS origin 정상 적용
```

**참고 문서**:
- `FRONTEND_INTEGRATION_SUMMARY.md` - 이슈 #2
- `BACKEND_INTEGRATION_STATUS.md` - 완료 작업 #2

---

## 🟡 알려진 제한사항

### LIMITATION-001: HTTP Method 불일치 (PUT vs PATCH)

**분류**: 설계 일관성
**우선순위**: 🟡 중간
**발견일**: 2025-10-31
**상태**: 문서화됨, 향후 개선 검토

**현황**:
- API 명세서: `PATCH /members/:id` (부분 수정)
- 백엔드 실제 구현: `PUT /subjects/:id` (전체 교체)
- 프론트엔드 구현: `PUT /subjects/:id` (백엔드에 맞춤)

**영향**:
- 기능적으로는 정상 작동
- RESTful API 베스트 프랙티스와 다소 불일치
- API 명세서와 실제 구현 간 차이

**권장 사항**:
1. **옵션 A**: 백엔드를 `PATCH`로 변경 (RESTful 권장)
   - 장점: 표준 준수, 의미가 명확함
   - 단점: 백엔드 및 프론트엔드 수정 필요

2. **옵션 B**: API 명세서를 `PUT`으로 수정
   - 장점: 현재 구현과 일치
   - 단점: RESTful 원칙과 다소 상이

3. **옵션 C**: 현상 유지
   - 장점: 수정 작업 불필요
   - 단점: 일관성 부족

**조치**:
- 현재는 `PUT` 방식으로 통일되어 정상 작동 중
- 향후 리팩토링 시 검토 예정

---

### LIMITATION-002: S3 버킷 생성 권한 부족

**분류**: 인프라
**우선순위**: 🟢 낮음
**발견일**: 2025-10-31
**상태**: 대체 방안 적용 완료

**현황**:
- 프론트엔드팀 요청: 새 S3 버킷 "golf-private" 생성
- AWS IAM 사용자 권한: `s3:CreateBucket` 권한 없음
- 대체 방안: 기존 "sppb-private" 버킷 사용

**영향**:
- 기능적 영향 없음
- 리소스 분리는 되지 않음

**해결 방법**:
1. AWS 관리자에게 `s3:CreateBucket` 권한 요청
2. 또는 기존 버킷 계속 사용

**현재 상태**:
- `AWS_S3_BUCKET=sppb-private` (기존 버킷 사용 중)
- S3 업로드 정상 작동 확인

---

## 📊 이슈 통계

### 전체 통계
- 총 이슈: 2개
- 해결 완료: 2개 (100%)
- 진행 중: 0개 (0%)
- 미해결: 0개 (0%)

### 우선순위별
- 🔴 높음: 2개 (모두 해결)
- 🟡 중간: 0개
- 🟢 낮음: 0개

### 분류별
- API 파라미터: 1개 (해결)
- 설정/환경: 1개 (해결)
- 제한사항: 2개 (문서화)

---

## 📝 이슈 템플릿

새로운 이슈 발견 시 아래 템플릿을 사용하여 기록합니다.

```markdown
### ISSUE-XXX: [이슈 제목]

**우선순위**: 🔴/🟡/🟢
**발견일**: YYYY-MM-DD
**해결일**: YYYY-MM-DD (해결 시)
**담당자**: [이름]

**문제 설명**:
[상세 설명]

**영향**:
- [영향 1]
- [영향 2]

**해결 방법**:
1. [단계 1]
2. [단계 2]

**수정 파일**:
- `파일경로 1`
- `파일경로 2`

**테스트 결과**:
```
[테스트 결과]
```

**참고 문서**:
- [관련 문서 링크]
```

---

## 🔍 이슈 검색 인덱스

### 키워드별 이슈
- **swingType**: ISSUE-001
- **CORS**: ISSUE-002
- **HTTP Method**: LIMITATION-001
- **S3**: LIMITATION-002
- **golf-swing**: ISSUE-001
- **포트**: ISSUE-002

### 파일별 이슈
- `golf-swing.controller.ts`: ISSUE-001
- `UploadSwingVideoUseCase.ts`: ISSUE-001
- `.env`: ISSUE-002

---

**마지막 업데이트**: 2025-10-31
**다음 검토일**: 프론트엔드 통합 테스트 후
